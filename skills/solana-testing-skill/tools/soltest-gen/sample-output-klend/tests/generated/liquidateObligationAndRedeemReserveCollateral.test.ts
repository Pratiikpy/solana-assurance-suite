// AUTO-GENERATED by soltest-gen — adversarial scaffold for `liquidateObligationAndRedeemReserveCollateral`.
// Fill the TODO fixtures (program load + account data), then run.
// Layer: LiteSVM integration. See solana-testing skill: litesvm-integration.md
import { LiteSVM } from "litesvm";
import { Keypair } from "@solana/kit";

const PROGRAM = "kamino_lending";

describe("liquidateObligationAndRedeemReserveCollateral", () => {
  // TODO: const svm = new LiteSVM(); svm.addProgramFromFile(programId, "target/deploy/kamino_lending.so");

  it("succeeds on the happy path", () => {
    // TODO: build liquidateObligationAndRedeemReserveCollateral with valid signers + accounts; assert success.
  });

  // NEGATIVE — missing-signer: the drain class. Pass `liquidator` as a non-signer.
  it("rejects when `liquidator` does not sign", () => {
    // build ix with liquidator.isSigner = false; expect the tx to FAIL.
    // assert on FailedTransactionMetadata (MissingRequiredSignature / custom).
  });

  // NEGATIVE — account substitution: `obligation` owned by the wrong program.
  it("rejects a substituted `obligation` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `repayReserve` owned by the wrong program.
  it("rejects a substituted `repayReserve` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `repayReserveLiquiditySupply` owned by the wrong program.
  it("rejects a substituted `repayReserveLiquiditySupply` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `withdrawReserve` owned by the wrong program.
  it("rejects a substituted `withdrawReserve` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `withdrawReserveCollateralMint` owned by the wrong program.
  it("rejects a substituted `withdrawReserveCollateralMint` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `withdrawReserveCollateralSupply` owned by the wrong program.
  it("rejects a substituted `withdrawReserveCollateralSupply` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `withdrawReserveLiquiditySupply` owned by the wrong program.
  it("rejects a substituted `withdrawReserveLiquiditySupply` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `withdrawReserveLiquidityFeeReceiver` owned by the wrong program.
  it("rejects a substituted `withdrawReserveLiquidityFeeReceiver` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `userSourceLiquidity` owned by the wrong program.
  it("rejects a substituted `userSourceLiquidity` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `userDestinationCollateral` owned by the wrong program.
  it("rejects a substituted `userDestinationCollateral` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — account substitution: `userDestinationLiquidity` owned by the wrong program.
  it("rejects a substituted `userDestinationLiquidity` (wrong owner)", () => {
    // create a look-alike account with owner = a different program id; expect FAIL.
  });

  // NEGATIVE — arithmetic boundary on `liquidityAmount` (release builds wrap, not panic).
  it("handles `liquidityAmount` at u*::MAX / 0 without wrapping", () => {
    // call with liquidityAmount = MAX and liquidityAmount = balance+1; assert reject (checked_* path).
  });

  // NEGATIVE — arithmetic boundary on `minAcceptableReceivedLiquidityAmount` (release builds wrap, not panic).
  it("handles `minAcceptableReceivedLiquidityAmount` at u*::MAX / 0 without wrapping", () => {
    // call with minAcceptableReceivedLiquidityAmount = MAX and minAcceptableReceivedLiquidityAmount = balance+1; assert reject (checked_* path).
  });

  // NEGATIVE — arithmetic boundary on `maxAllowedLtvOverridePercent` (release builds wrap, not panic).
  it("handles `maxAllowedLtvOverridePercent` at u*::MAX / 0 without wrapping", () => {
    // call with maxAllowedLtvOverridePercent = MAX and maxAllowedLtvOverridePercent = balance+1; assert reject (checked_* path).
  });
});
